#pragma once

namespace MetaNN
{
/// data types
struct CategoryTags
{
    struct Scalar;
    struct Matrix;
    struct BatchScalar;
    struct BatchMatrix;
};

/// device types
struct DeviceTags
{
    struct CPU;
};
}
